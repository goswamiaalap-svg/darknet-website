export const metadata = {
  title: "Events",
  description: "Browse all CTFs, hackathons, workshops, and seminars organized by The Darknet Community.",
};

export default function EventsLayout({ children }) {
  return children;
}
