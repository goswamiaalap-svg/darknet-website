export const metadata = {
  title: "Chapters",
  description: "The Darknet Community chapters across India, driving cybersecurity education locally.",
};

export default function ChaptersLayout({ children }) {
  return children;
}
